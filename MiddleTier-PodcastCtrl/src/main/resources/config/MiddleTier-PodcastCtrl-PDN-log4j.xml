<!--suppress XmlUnboundNsPrefix -->
<configuration>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/var/log/jbr/MiddleTier-PodcastCtrl-PDN.log</file>

        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%-10.10thread] %highlight(%-5level) %cyan(%-36.36logger{36}) - %msg%n</pattern>
        </encoder>

        <rollingPolicy  class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- rollover daily -->
            <fileNamePattern>/var/log/jbr/MiddleTier-PodcastCtrl-PDN-%d{yyyy-MM-dd}.%i.log
            </fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy
                    class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
    </appender>

    <appender name="Database" class="ch.qos.logback.classic.db.DBAppender">
        <connectionSource class="ch.qos.logback.core.db.DriverManagerConnectionSource">
            <driverClass>com.mysql.cj.jdbc.Driver</driverClass>
            <url>${db.pdn.log.server}?useSSL=false</url>
            <user>loginsert</user>
            <password>rIYpQtmG</password>
        </connectionSource>
    </appender>

    <logger name="com.jbr.middletier.podcast.manage">
        <level value="Database"/>
        <appender-ref ref="Database"/>
    </logger>

    <root level="info">
        <appender-ref ref="FILE" />
    </root>
</configuration>
